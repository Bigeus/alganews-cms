import styled from "styled-components";

export const Wrapper = styled.div`
    display: flex;
    flex-direction: row;
    padding: 8px;
    gap: 10px;
    
    font-size: 14px;
`

export const WordCount = styled.h2`
    color: #274060;
    text-transform: uppercase;
`

export const Value = styled.h2`
    color: #09f;
`